import 'package:flutter/material.dart';

class ParkingSpacesView extends StatelessWidget {
  const ParkingSpacesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("View all parkingspaces"));
  }
}